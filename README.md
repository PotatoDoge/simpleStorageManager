Simple storage manager made in JAVA and SQL (using MySQL server).
GUI made with SceneBuilder/JavaFX
----------------------------------
If you use this, make sure to add the mysql.jar file to the project's path. 
----------------------------------
IGNORE THE 'OUT' FOLDER

